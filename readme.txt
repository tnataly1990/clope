Формат данных для файла data.txt:
объекты в рамках одной транзакции разделены пробелами, транзакции разделены символом перевода строки.

При запуске программы следует указать два параметра: путь и имя файла с транзакциями, параметр repulsion.
Пример ввода входных параметров: data.txt 2